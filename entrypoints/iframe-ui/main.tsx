import React from 'react';
import ReactDOM from 'react-dom/client';
import { CommandDemo } from './suggestions-menu-popover-frame.tsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CommandDemo />
  </React.StrictMode>,
);
